main
array[5] x, y;
var a, b, c, d, e, f, g, h; {
 let a <- call InputNum();
 let b <- x[1];
 let c <- x[2];
 let x[3] = b + a;
 let e <- x[3];
 if a < 0 then 
    let x[3] <- c; 
    if b > 0 then
        let d <- d + e; 
    else 
        let y[3] <- d + x[3]; 
    fi;
 else 
    let e = c + 1; fi

call OutputNum(x[3]);
call OutputNum(y[3]);
call OutputNum(a);
call OutputNum(c);
call OutputNum(e);
call OutputNum(f);
call OutputNum(g);


}.