main
var i, x, y;
{
    let i <- 1;
     while i>=0 do
        let y <- x + 1;
        while i>=0 do
            let y <- i + 10;
            let y <- x + 1;
        od;
    od;
    call OutputNum(i);
    call OutputNum(x);
    call OutputNum(y);
}.