main 
array[4] a, arra;

var x,y,i,j; 
{
    let i<-call InputNum(); 
    let x<-0; 
    let j<- a[1]; 
    call OutputNum(arra[1]);

    if x != 0 then
        let x <- arra[1] + 1; 
        let arra[1] <- j + 1;       
		call OutputNum(a[1]);
        call OutputNum(arra[1]);
    else
        call OutputNum(a[1]);
        let a[1] <- 1;
        call OutputNum(a[1]);
        call OutputNum(arra[1]);
    fi; 
}.