main
var a, b, c, d, e; {
 let a <- call InputNum();
 let b <- a;
 let c <- b;
 let d <- b + c;
 let e <- a + b;
 if a < 0 then 
    let c <-c + 1;
    if b > 0 then
        let d <- d + e; 
    else 
        let d <- d + a; fi
 else 
    let d <- d + 1; fi

call OutputNum(d);
call OutputNum(b);
call OutputNum(a);
call OutputNum(c);

}.