main
var i, j, x, y;
array[4] a, b;
{
    let i <- call InputNum();
    let j <- b[5]; 
    let a[5] <- 5;
    let y <- b[5]
    call OutputNum(i);
    call OutputNum(j);
    call OutputNum(x);
    call OutputNum(y);

}.
