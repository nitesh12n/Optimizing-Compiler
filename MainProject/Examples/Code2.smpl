main
var a, b;
array[3] q;
var x, y;
array[3] r;

{
	let a <- call InputNum();
	let x <- a + 1;
	let x <- a + 2;
    let y <- a + 2;

	if a + 100*5< 0 + 0 +100*a then
		let x <- a + 2;
		let q[1] <- a;
	else
		let x <- a + 3;
		let q[1] <- a;
		let r[1] <- a;
	fi;
    let y <- a + 3;
    let y <-a+2;
    let a <- x + 1;
	let q[1] <- a;
	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(x);
	call OutputNum(y);
	call OutputNum(q[1]);
	call OutputNum(r[1]);

}.