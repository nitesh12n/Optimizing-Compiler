main 
array[4] a, b, c;

var x,y,i,j; 
{
    let i<-call InputNum(); 
    let x<-0; 
    let j<- a[1]; 
    call OutputNum(a[1]);

    while x != 0 do
        let x <- a[1] + 1; 
        let a[1] <- j + 1;             
        if j < 0 then 
            let j <- x + 1; 
            let b[1] <- i + 1;  
         else 
            let i <-i + 1; 
            let c[1] <- i + 1;  
        fi; 
		call OutputNum(a[1]);
        call OutputNum(b[1]);
        call OutputNum(c[1]);
    od; 
}.