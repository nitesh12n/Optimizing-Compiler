main
var i, x, y;
array[4] a, b, c;
{
    let i <- a[1];
    let x <- b[1]; 
    let y<-  c[1];

    while i>=0 do
        let a[i] <- x - 1;
        let y <- c[1] * 2;
        let y <- c[1] + 2;
        let y <- b[1] - 2;
        let x <- a[1]/2;

    od;
    call OutputNum(i);
    call OutputNum(x);
    call OutputNum(y);
    call OutputNum(a[i]);
    call OutputNum(b[i]);
    call OutputNum(c[i]);
}.